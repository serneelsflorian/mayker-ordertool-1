---
description: One-time project setup — connect MCPs, import features, generate feature map, status mapping, CI pipelines
argument-hint: "(no arguments)"
model: sonnet
---

Use the **init-project** skill at `@.claude/skills/init-project/SKILL.md` and follow it exactly. This is an interactive, local, one-time setup — do not run it in an unattended surface.

Arguments (if any): $ARGUMENTS
